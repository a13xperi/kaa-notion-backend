# Frontend Task

## Zone
Frontend (kaa-app/src)

## Stack
- React 18 + TypeScript
- TailwindCSS
- React Query for data fetching
- React Router for navigation

## Task
{{TASK}}

## Requirements
{{REQUIREMENTS}}

## Constraints
- Follow existing component patterns in kaa-app/src/components
- Use existing hooks from kaa-app/src/hooks
- Maintain TypeScript strict mode compliance
- Use TailwindCSS for styling (no inline styles)
- Components must be accessible (ARIA attributes)

## Verification
1. Run tests: `cd kaa-app && npm test`
2. Check types: `cd kaa-app && npx tsc --noEmit`
3. Check lint: `cd kaa-app && npm run lint`
4. Visual check: `cd kaa-app && npm start`

## Success Criteria
- [ ] All requirements implemented
- [ ] Tests pass
- [ ] No TypeScript errors
- [ ] No linting errors
- [ ] Components render correctly

When ALL criteria met, output: RALPH_COMPLETE
