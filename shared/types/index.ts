/**
 * SAGE Platform - Shared Types
 *
 * Export all shared types for use in both backend and frontend.
 * Import in frontend: import { Project, ApiResponse } from '../../../shared/types'
 * Import in backend: import { Project, ApiResponse } from '../../shared/types'
 */

export * from './api';
